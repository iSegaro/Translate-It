export const REGION_COMPARISON_CONFIGURATIONS = Object.freeze([
  Object.freeze({ scale: 1 }),
  Object.freeze({ scale: 1.25 }),
  Object.freeze({ scale: 1.5 }),
  Object.freeze({ scale: 1.75 }),
  Object.freeze({ scale: 2 })
])
