/**
 * Internal extension application URL paths.
 * Single source of truth for extension pages opened via shared launcher.
 */
export const EXTENSION_APPS = {
  subtitle: {
    urlPath: 'src/html/subtitle.html',
  },
  pdf: {
    urlPath: 'src/html/pdf.html',
  },
};
