export const STT_PROVIDER_MODES = Object.freeze({
  BATCH: 'batch',
  STREAMING: 'streaming'
});

export const STT_PROVIDER_EXECUTION_LOCATIONS = Object.freeze({
  BACKGROUND: 'background',
  OFFSCREEN: 'offscreen'
});

export default {
  STT_PROVIDER_MODES,
  STT_PROVIDER_EXECUTION_LOCATIONS
};
